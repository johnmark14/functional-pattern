{% comment %}

This snippet generates the structure of a comment.
It is used to show not only recent comments, but also comments that were
just published but not yet approved.

More information:
- http://docs.shopify.com/themes/liquid-variables/comment

{% endcomment %}

<div class="comment-col">
  <div class="blogs-sub-title">
    <p class="blog-date">
      <i class="far fa-calendar"></i> <time datetime="{{ comment.created_at | date: '%Y-%m-%dT%H:%M:%SZ' }}">{{ comment.created_at | date: format: 'date' }}</time>            
    </p>

    <p class="author">
      <i class="far fa-user"></i>
      {{ comment.author }}
    </p>
  </div>  

  <div class="rte">
    {{ comment.content }}
  </div>
</div>